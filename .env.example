# Optional — leave blank to disable push notifications
NTFY_URL=
NTFY_TOPIC=
NTFY_TOKEN=
# Optional: absolute path to agent / cursor-agent if not on PATH
# CURSOR_AGENT_PATH=
# Optional: absolute path to codex if not on PATH / AppData cache
# CODEX_CLI_PATH=
# Optional: override active harness from auto-rob.config.json (cursor | codex)
# AUTO_ROB_HARNESS=cursor
